<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Trạng thái khi được nhấn -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>  <!-- Giữ nguyên màu nền trắng -->
            <stroke android:width="2dp" android:color="#000000"/>  <!-- Nét liền màu chính khi được nhấn -->
            <corners android:radius="30dp"/>
            <padding android:left="10dp" android:top="6dp" android:right="10dp" android:bottom="6dp"/>
        </shape>
    </item>
    <!-- Trạng thái khi được focus -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="#FFFFFF"/>
            <stroke android:width="3dp" android:color="#000000"/>
            <corners android:radius="30dp"/>
            <padding android:left="10dp" android:top="6dp" android:right="10dp" android:bottom="6dp"/>
        </shape>
    </item>
    <!-- Trạng thái chưa được nhấn - bình thường -->
    <item>
        <shape>
            <solid android:color="#FFFFFF"/>  <!-- Giữ nguyên màu nền trắng -->
            <stroke android:width="1dp" android:color="#000000" android:dashWidth="10dp" android:dashGap="5dp"/>  <!-- Đường viền nét đứt -->
            <corners android:radius="30dp"/>
            <padding android:left="10dp" android:top="6dp" android:right="10dp" android:bottom="6dp" />
        </shape>
    </item>
</selector>
